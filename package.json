{
  "name": "viport",
  "version": "0.1.0",
  "private": true,
  "description": "Systemd/systemctl service control & port visualiser",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web"
  }
}
